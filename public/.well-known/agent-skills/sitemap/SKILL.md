# sitemap

Publishes and refreshes the XML sitemap used by search engines and agents.

## Endpoint

- `GET /sitemap.xml`

## Behavior

- Lists canonical URLs for the homepage and machine-readable discovery resources.
- Regenerated automatically during `npm run build`.

