# oauth-protected-resource

Publishes OAuth Protected Resource Metadata for API token discovery.

## Endpoint

- `GET /.well-known/oauth-protected-resource`

## Behavior

- Declares the resource identifier, authorization servers, and supported scopes.

