# content-signals

Publishes content-usage preferences for AI systems.

## Endpoint

- `GET /robots.txt`

## Behavior

- Includes `Content-Signal` directives:
  - `ai-train=no`
  - `search=yes`
  - `ai-input=no`

